#include <stdio.h>

int main() {
    // массив хранит название файла и слова
    char lines[5][30] = {
        "/test.txt",
        "Привет",
        "Каша",
        "Как",
        "Дела"
    };
    
    printf("Имя файла: %s\n", lines[0]);
    
    char content[120];
    int index = 0;
    
    //В данном цикле слова из "файла" переносим в переменную content
    // Каждое новое слово начинает с новой строки
    for (int i = 1; i < 5; i++) {
        
        int j = 0;
        while (lines[i][j] != '\0') {
            content[index] = lines[i][j];
            index++;
            j++;
        }    
        content[index] = '\n';
        index++;
    }
    content[index] = '\0';
    printf("Содержимое файла:\n%s", content);
    
    char target[] = "Привет"; //слово которое надо найти
    int contentLength = 0, targetLength = 0; // первая переменная для счета кол-во символов в content, а вторая target
    
    // Считаем длину content, результат contentLength
    while (content[contentLength] != '\0') contentLength++;


    // Считаем длину target,результат targetLength
    while (target[targetLength] != '\0') targetLength++;

    // Ищем target в content
    int found = 0;
    for (int i = 0; i <= contentLength - targetLength; i++) {
        int match = 1;
        for (int j = 0; j < targetLength; j++) {
            if (content[i + j] != target[j]) {
                match = 0;
                break;
            }
        }
        if (match) {
            found = 1;
            break;
        }
    }

    if (found) {
        printf("\nСлово \"%s\" найдено в тексте\n", target);
    } else {
        printf("\nСлово \"%s\" не найдено\n", target);
    }

    return 0;
}
